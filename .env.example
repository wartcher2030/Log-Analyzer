# Example environment variables for local development and Vite builds
# Create a file named .env.local (or use your CI environment variables) and add the real value.

# Use VITE_ prefix so Vite exposes the var during build as import.meta.env.VITE_GEMINI_API_KEY
VITE_GEMINI_API_KEY=

# If you keep a non-prefixed var for compatibility, Vite will map it too (see vite.config.ts)
GEMINI_API_KEY=
